[]add todo list to github

[]introduce learn rate scheduler

scheduler = torch.optim.lr_scheduler.StepLR(
    optimizer,
    step_size=5,
    gamma=0.5
)

[]Load model later as necessary
model.load_state_dict(torch.load("best.pt"))


[]Monitor GPU usage
import torch

print(torch.cuda.is_available())
print(torch.cuda.current_device())
print(torch.cuda.get_device_name(0))

print(torch.cuda.memory_allocated(0)/1e9)	#GB
print(torch.cuda.memory_reserved(0)/1e9)	#GB

import subprocess

result = subprocess.check_output(
    [
        "nvidia-smi",
        "--query-gpu=utilization.gpu,memory.used,memory.total",
        "--format=csv,noheader,nounits"
    ]
)

print(result.decode())

[]create a csv with training information




